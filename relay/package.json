{
  "name": "sanctum-relay",
  "version": "1.0.0",
  "description": "Sanctum Siege — TikTok WebSocket relay + dev CLI",
  "type": "module",
  "scripts": {
    "relay": "npx tsx index.ts",
    "cli": "npx tsx cli.ts",
    "dev": "npx concurrently -n relay,cli 'npm run relay' 'npm run cli'"
  },
  "dependencies": {
    "tiktok-live-connector": "^2.4.0",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^6.0.3"
  }
}
